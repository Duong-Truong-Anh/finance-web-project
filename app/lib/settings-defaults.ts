export type { Theme } from '@/src/lib/settings/repository';
export type { Currency } from '@/src/lib/currency/types';

export const DEFAULT_THEME = 'g90' as const;
export const DEFAULT_CURRENCY = 'VND' as const;
